# Goal: <title>

- **Slug:** <kebab-slug>   **Created:** <date>   **Status:** draft | ready | active | done | parked

> `/autopilot` will not start until **Status: ready** — i.e. criteria are testable, Open questions is
> empty, and Required inputs are provided-or-deferred. `/spec` sets this.

## Problem / why now
<one paragraph — what's broken or missing, and why it's worth doing now>

## Acceptance criteria (the contract)
> Concrete and testable. Vague criteria make autonomy churn.
- [ ] <a passing test / a command's output / a visible behavior>
- [ ] ...

## Constraints
- Stack / budget / deadline / non-negotiables:

## Out of scope (YAGNI)
- <explicitly not doing this now>

## Research agenda  (filled in /spec FRAME; answered by /research → docs/domain/)
> What we must learn before the solution spec is trustworthy. Each becomes a docs/domain/ topic.
- [ ] <domain rule / API specific / compliance / data shape / "how pros actually do this">
- [ ] ...

## Open questions  (must be empty to be `ready`)
- [ ] <decision/fork the human must resolve before building — e.g. which provider first>

## Required inputs  (the human must supply these)
- [ ] <account / API key / credential / data source> — sandbox: …, live: … (or: deferred because …)

## Checkpoints — STOP and ask the human before:
- <e.g. placing any real order · writing to a live ledger · sending real email/SMS · deploying to prod · deleting data · adding a paid dependency · an ambiguous product trade-off>

## Definition of done
<the single sentence that means "ship it">
